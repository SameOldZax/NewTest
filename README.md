# Repeat Bot

ربات ساده تلگرام:
1. یه پیام (متن/عکس/موزیک/ویدیو/فایل) بفرست.
2. بازه‌ی دقیقه رو بهش بگو (مثلاً 5).
3. هر N دقیقه همون پیام رو برات دوباره می‌فرسته، با دکمه «🛑 توقف ارسال».

## اجرا
متغیر محیطی `BOT_TOKEN` رو با توکن گرفته‌شده از @BotFather ست کن، بعد:

```
npm install
npm start
```
